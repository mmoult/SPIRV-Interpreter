gl_FragCoord: [ 3.0, 6.0, 0.0, 0.0 ]

uni_txtr:
    ref: "noise.png"
    dim: [ 7, 7 ]
    mipmaps: 1
    # Two below fields ("comps" and "data") unused because data is instead provided through the file (path in "ref")
    comps: 1
    data: []
